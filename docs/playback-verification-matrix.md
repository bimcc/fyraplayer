# Playback Verification Matrix

> Created: 2026-05-17  
> Purpose: make real playback verification repeatable across protocols, browsers, and stream backends.

This document is the evidence log for `CR-005`. Unit tests prove code-level behavior; this matrix proves browser playback behavior. Do not mark a protocol/browser combination as passed without a dated run record.

---

## 1. How To Run

1. Start the demo:

```bash
pnpm dev:vite
```

2. Open the demo URL printed by Vite, usually:

```text
http://localhost:3000/
```

3. Select the source from `examples/sources.js` or enter the stream URL manually.
4. Record the browser, OS, source, expected events, actual events, and result in section 6.

For local MediaMTX WebRTC/HLS checks:

```bash
G:\MTX\mediamtx.exe
```

Publish from OBS with:

```text
Server: rtmp://127.0.0.1:1935/live
Stream key: test
```

The expected local playback URLs are:

```text
HLS:    http://127.0.0.1:8888/live/test/index.m3u8
WHEP:   http://127.0.0.1:8889/live/test/whep
RTMP:   rtmp://127.0.0.1:1935/live/test
```

The same evidence can be collected on project-specific MediaMTX ports. On
2026-05-19 the local MediaMTX instance used `21935` RTMP, `28888` HLS, `28889`
WebRTC/WHEP, and API `9997`.

Browser verification should use HLS through FyraPlayer/hls.js and WHEP through `tech-webrtc`; direct browser address-bar playback of `.m3u8` does not prove HLS support.

MediaMTX codec note:

- Browser WebRTC audio should be validated with an ingest path that provides Opus-compatible audio to WebRTC. OBS RTMP commonly publishes AAC audio, which can work through HLS while leaving the browser WebRTC audio track present but muted. MediaMTX documents an OBS WebRTC-readable publishing path through RTSP with H.264 settings and `libopus` audio.
- If FyraPlayer emits `network.code: 'WEBRTC_AUDIO_MUTED'`, first check the MediaMTX/OBS codec path before treating it as a player volume defect.

MediaMTX HLS note:

- Use `lowLatency: false` for the normal HLS URL unless the run is explicitly testing LL-HLS. FyraPlayer forces normal HLS into hls.js buffered live mode (`lowLatencyMode: false`, `liveSyncMode: 'buffered'`) because hls.js 1.6.x defaults to low-latency edge chasing. This avoids treating a normal live stream with separate audio renditions like an LL-HLS stream.
- If audio repeats or layers while video moves forward and VLC shows the same symptom, first check OBS audio routing. The 2026-05-18 local issue was confirmed as OBS desktop-audio capture feedback, not a FyraPlayer/MediaMTX/HLS defect.
- OBS checklist: mute or remove `Desktop Audio` / browser `Application Audio Capture` when the browser is playing the stream, keep only the intended media/MP4 source audio active, set Advanced Audio Properties monitoring to `Monitor Off` unless deliberately monitoring, and avoid playing an audible HLS preview on the same desktop session OBS is capturing.
- In the demo console, `window.fyraPlayer` exposes the active player. The important HLS config evidence is `lowLatencyMode=false` for the normal MediaMTX preset and `lowLatencyMode=true` only when the LL-HLS preset or `source.lowLatency=true` is selected.

Interruption UI note:

- The optional UI shell intentionally uses one generic user-facing message for recoverable playback interruption: `视频流中断，正在重新连接...`.
- When reconnect attempts are exhausted it shows `视频流中断，请刷新或重试`.
- Do not split this wording by live/VOD until the public Source or Tech contract exposes a reliable live-state flag. Today `FMP4Source.isLive` exists, while HLS/DASH live/VOD state is Tech/manifest specific.

HLS fMP4/CMAF note:

- `https://devstreaming-cdn.apple.com/videos/streaming/examples/img_bipbop_adv_example_fmp4/master.m3u8` is included in the demo presets as `Apple HLS fMP4/CMAF sample`.
- Treat it as HLS with fMP4/CMAF segments. It validates the HLS Tech path, not the direct no-manifest `FMP4Tech` path.
- Previous Chrome local-origin testing blocked this Apple sample by CORS from the local demo origin. Record CORS as an environment result rather than marking playback support failed.

---

## 2. Automatic Checks

These checks do not replace browser playback runs, but they keep the verification assets healthy:

```bash
pnpm check:sources
pnpm check:public-api
pnpm check:exports
pnpm test -- --runInBand
```

`pnpm check:sources` validates the structure of `examples/sources.js` so the demo matrix does not silently drift into invalid source definitions.

`createPerformanceMonitorPlugin()` can be enabled during manual runs to record
FPS, latency, pending queue, and buffer-budget warnings. A
`PERFORMANCE_BUDGET` QoS warning is evidence of a breached threshold, not an
automatic protocol failure; include the metric, value, threshold, browser, and
stream when logging it.

For the 30-minute live run, use tool-assisted sampling plus a short manual observation note:

- preferred scripted path:
  `pnpm long-run:browser -- --source "HLS demo" --duration 30m --interval 10s --out .fyra-long-run/hls-edge-30m.json`;
- assert the scripted or manual JSON report with:
  `pnpm long-run:assert -- .fyra-long-run/hls-edge-30m.json --require-tech hls --min-samples 150 --min-duration-sec 1740`;
- use `--source-url` / `--source-type` for project or MediaMTX URLs, and use
  `--expect-live` for live streams that must not end or stall;
- in the demo page console, start sampling with `window.fyraLongRun.start(10000)` and stop with `window.fyraLongRun.stop()`;
- export samples with `window.fyraLongRun.getJson()`;
- sample every 5 to 10 seconds;
- record player state, source, Tech, currentTime, video readyState, resolution, buffered range, total/dropped frames, `document.querySelectorAll('video').length`, `document.querySelectorAll('audio').length`, `document.querySelectorAll('fyra-ui-shell').length`, and Chrome heap when available;
- include user-visible observations such as stalls, reconnect prompt visibility, audio/video sync, and whether playback recovered without page refresh;
- save the result and assertion summary as a dated row in section 6. Manual
  watching alone is not enough for commercial acceptance because memory/listener
  growth and dropped-frame trends need sampled evidence.

---

## 3. Browser Scope

| Browser | Required For | Notes |
|---|---|---|
| Chrome latest | WebRTC, HLS via hls.js, DASH, ws-raw fallback, MP4, UI | Primary commercial validation browser |
| Edge latest | WebRTC, HLS via hls.js, DASH, ws-raw fallback, MP4, UI | Chromium cross-check |
| Safari latest | Native HLS, MP4, basic UI | WebCodecs and MSE behavior may differ; record unsupported features explicitly |
| Firefox latest | HLS via hls.js, DASH, MP4 | WebRTC behavior may require backend-specific signaling checks |

Use exact browser versions in the run log.

---

## 4. Protocol Matrix

| Area | Source | Browser Target | Expected Events | Pass Criteria | Status |
|---|---|---|---|---|---:|
| HLS VOD | `https://sf1-cdn-tos.huoshanstatic.com/obj/media-fe/xgplayer_doc_video/hls/xgplayer-demo.m3u8` | Chrome, Edge, Safari | `ready`, `play`, `stats`; no fatal `network` | Starts playback, seek works if VOD duration is exposed | Chrome + Edge pass; Safari pending |
| HLS fMP4/CMAF VOD | `https://devstreaming-cdn.apple.com/videos/streaming/examples/img_bipbop_adv_example_fmp4/master.m3u8` | Chrome, Edge, Safari | `ready`, `play`, `stats`; no fatal `network` unless blocked by CORS | Starts playback as HLS with fMP4/CMAF segments; CORS block is recorded as environment limitation | Edge pass; Chrome local demo was previously CORS-limited; Safari pending |
| HLS alternate | `https://bitdash-a.akamaihd.net/content/sintel/hls/playlist.m3u8` | Chrome, Edge, Safari | `ready`, `play`, `stats`; no fatal `network` | Starts playback and can recover after pause/play | pending |
| DASH VOD | `https://dash.akamaized.net/akamai/bbb_30fps/bbb_30fps.mpd` | Chrome, Edge, Firefox | `ready`, `play`, `stats`, optional `levelSwitch` | Starts playback and emits stats within 10 seconds | Chrome + Edge pass; Firefox pending |
| DASH alternate | `https://bitmovin-a.akamaihd.net/content/sintel/sintel.mpd` | Chrome, Edge, Firefox | `ready`, `play`, `stats`; no fatal `network` | Starts playback and pause/play works | pending |
| HLS quality control | `https://test-streams.mux.dev/x36xhzz/x36xhzz.m3u8` | Chrome, Edge, Safari | `ready`, `stats`, `levelSwitch`, `getQualityState()` | Exposes multiple quality levels; manual level selection disables ABR; Auto restores ABR | Chrome pass; Edge/Safari pending |
| DASH quality control | `https://dash.akamaized.net/akamai/bbb_30fps/bbb_30fps.mpd` | Chrome, Edge, Firefox | `ready`, `stats`, `levelSwitch`, `getQualityState()` | Exposes multiple representations; manual representation selection disables ABR; Auto restores ABR | Chrome manual pass; Edge quality-state smoke pass; Firefox pending |
| MP4 file | `https://sf1-cdn-tos.huoshanstatic.com/obj/media-fe/xgplayer_doc_video/mp4/xgplayer-demo-360p.mp4` | Chrome, Edge, Safari, Firefox | `ready`, `play`, `ended` when allowed to finish | Starts playback and seeking works | Chrome + Edge pass; Safari/Firefox pending |
| HTTP-FLV / ws-raw fallback | `https://sf1-cdn-tos.huoshanstatic.com/obj/media-fe/xgplayer_doc_video/flv/xgplayer-demo-360p.flv` | Chrome, Edge | `ready`, `play`, `stats`; no fatal `network` | MSE fallback starts playback; no unbounded error loop | Chrome pass; Edge pending |
| WebRTC WHEP local | `http://127.0.0.1:8889/live/test/whep` or custom `http://127.0.0.1:28889/live/test/whep` | Chrome, Edge | `ready`, `play`, `network`, `stats` | Starts under MediaMTX with a published stream; destroy/recreate is clean; disconnect/reconnect behavior recorded separately | Chrome pass; Edge published-stream pass on custom port; Edge 404/error-path pass; interruption pending |
| HLS local MediaMTX | `http://127.0.0.1:8888/live/test/index.m3u8` or custom `http://127.0.0.1:28888/live/test/index.m3u8` | Chrome, Edge, Safari | `ready`, `play`, `stats` | Starts under MediaMTX with a published stream; normal HLS uses buffered hls.js live config unless `lowLatency: true` is explicitly selected | Chrome pass; Edge pass on custom port; Safari pending |
| LL-HLS local MediaMTX | `http://127.0.0.1:8888/live/test/index.m3u8` with `lowLatency: true` | Chrome, Edge, Safari | `ready`, `play`, `stats`; no fatal `network` | Starts under MediaMTX with explicit LL-HLS config; compare latency and smoothness separately from normal HLS | Chrome smoke pass; Edge/Safari pending |
| GB28181 gateway adapter | Project-specific invite/control endpoint returning FLV/TS/HLS/WebRTC/fMP4 | Chrome, Edge | `network`, `ready`, control responses | Backend invite returns a browser-playable URL and bye/ptz/query/keepalive behavior is recorded | unit adapter pass; real gateway pending |
| fMP4 direct | Local ffmpeg fixture `http://127.0.0.1:18080/stream.fmp4`; project-specific HTTP/WS fMP4 sources still need separate rows | Chrome, Edge | `ready`, `play`, `stats`, no quota errors | Buffer remains bounded for a 10 minute run | CR-008 unit backpressure/quota pass; CR-020 Edge fixture 10-minute pass and Chrome manual playback pass; project-specific streams pending |

---

## 5. Scenario Checklist

Run these for each stable protocol before promotion:

| Scenario | Expected Result | Status |
|---|---|---:|
| init -> ready -> play | Playback begins or autoplay block is surfaced as non-fatal network/error event | Chrome pass for HLS/DASH/MP4/ws-raw fallback and local MediaMTX HLS/WHEP; Edge pass for HLS, HLS fMP4/CMAF, DASH, and MP4 |
| pause -> play | No duplicate tech load, state returns to playing | Chrome browser pass for HLS; unit pass for Player lifecycle |
| seek for VOD | `currentTime` changes and playback resumes | Chrome browser pass for HLS VOD; unit pass for active Tech delegation |
| switchSource | Previous tech is destroyed; new source reaches ready/play | Chrome browser pass for HLS -> DASH; unit pass for old Tech event isolation |
| destroy -> recreate | DOM/event listeners do not duplicate; UI plugin cleans up | Chrome browser pass for DASH destroy -> HLS recreate and MediaMTX WHEP destroy -> recreate; UI cleanup unit pass |
| network interruption | Fatal event shape is recorded; reconnect/fallback behavior is deterministic; optional UI shows `视频流中断，正在重新连接...` during retry and clears it after recovery | unit pass for pending-timer clearing and same-Tech retry; UI message implemented; real MediaMTX interruption pending |
| 30 minute live run | Memory, listener count, media element count, dropped-frame trend, and reconnect behavior do not grow unbounded | Edge WHEP 30-minute assertion pass; Edge HLS 30-minute playable/recovered but zero-fatal retest pending; direct fMP4 fixture has 10-minute assertion pass |

---

## 6. Run Log

Append new rows; do not overwrite historical failures.

| Date | Tester | Browser / OS | Source | Scenario | Result | Evidence / Notes |
|---|---|---|---|---|---:|---|
| 2026-05-17 | Codex | Automated Node checks / Windows | `examples/sources.js` | Source structure validation | pass | `pnpm check:sources` |
| 2026-05-17 | Codex | Automated TypeScript/Jest / Windows | package exports + unit tests | API/export/unit verification | pass | `pnpm check:public-api`, `pnpm check:exports`, `pnpm test -- --runInBand` |
| 2026-05-17 | Codex | Jest unit test / Windows | HLS/DASH Tech event contracts | error/ready/levelSwitch semantics | pass | `pnpm test -- tests/hls-dash-events.test.ts --runInBand`; 5 tests covering HLS warning vs fatal mapping, DASH warning vs fatal mapping, ready de-duplication, and stable `levelSwitch` payloads |
| 2026-05-17 | Codex | Automated TypeScript/Jest / Windows | package build + unit tests + public API + source manifest | post-event-contract regression | pass | `pnpm exec jest --runInBand` passed, 11 suites / 40 tests; `pnpm check:public-api` passed; `pnpm build` passed; `pnpm check:sources` passed, 14 sources verified |
| 2026-05-17 | Codex | Documentation contract / Windows | API event docs | HLS/DASH public event semantics | pass | `docs/api.md` now documents tested `ready`, `network`, and `levelSwitch` semantics; `pnpm check:public-api` and `pnpm build` passed after the doc update |
| 2026-05-17 | Codex | Jest unit test / Windows | Player lifecycle | pause/play, seek, switchSource, destroy/recreate | pass | Added regression coverage for active Tech delegation, old Tech event detachment before `switchSource`, and no duplicated forwarding after destroy/recreate; `pnpm exec jest --runInBand` passed, 11 suites / 43 tests |
| 2026-05-17 | Codex | Chromium 148.0.7778.168 / Windows 10 | HLS VOD demo | pause -> play -> seek | pass | Vite demo on `http://127.0.0.1:4173/basic.html`; HLS reached `playing`, `1280x720`; pause state `paused` at 32.30s; resume returned to `playing`; seek target 12.00s resumed at 12.94s; no fatal `error`/`network` events |
| 2026-05-17 | Codex | Chromium 148.0.7778.168 / Windows 10 | HLS VOD -> DASH BBB VOD | switchSource | pass | Source switch from HLS to DASH reached `playing`, current source `dash`, `480x270`, `ready=1`, `play=1`, `levelSwitch=2`, no fatal events |
| 2026-05-17 | Codex | Chromium 148.0.7778.168 / Windows 10 | DASH BBB VOD -> HLS VOD demo | destroy -> recreate | pass | Destroyed DASH player returned old state to `idle`; recreated HLS reached `playing`, current source `hls`, `1280x720`, `ready=1`, `play=1`, `stats=1`, no fatal events |
| 2026-05-17 | Codex | Chrome 148.0.0.0 / Windows 10 | HLS VOD demo | init -> ready -> play -> stats | pass | Vite demo on `http://127.0.0.1:4173/basic.html`; reached `playing`, `currentTime=1.90s`, `1280x720`, events: `ready=1`, `play=1`, `stats=2`; hls.js startup buffer events surfaced as non-fatal `network` warnings |
| 2026-05-17 | Codex | Chrome 148.0.0.0 / Windows 10 | DASH BBB VOD | init -> ready -> play -> stats -> levelSwitch | pass | Reached `playing`, `currentTime=1.72s`, `480x270`, events: `ready=1`, `play=1`, `stats=1`, `levelSwitch=2`; `levelSwitch` payload normalized to stable small objects |
| 2026-05-17 | Codex | Chrome 148.0.0.0 / Windows 10 | MP4 demo | init -> ready -> play -> stats | pass | Reached `playing`, `currentTime=1.96s`, `640x360`, events: `ready=1`, `play=1`, `stats=2` |
| 2026-05-17 | Codex | Chrome 148.0.0.0 / Windows 10 | HTTP-FLV / ws-raw fallback demo | init -> ready -> play -> stats | pass | Reached `playing`, `currentTime=1.80s`, `640x360`, events: `ready=1`, `play=1`, `stats=1`; no fatal network loop observed in smoke run |
| 2026-05-17 | Codex | Chromium 148.0.7778.168 / Windows 10 | HTTP-FLV / ws-raw default MSE path | CR-009 stable pipeline contract | pass | Source had no `pipeline` or deprecated `experimental` opt-in, so default path is `pipeline: 'mse'`; reached playable video, `currentTime=1.82s`, `640x360`, `readyState=4`, events: `ready=1`, `stats=2`, no fatal `error`/`network` events |
| 2026-05-17 | Codex | Chrome 148.0.0.0 / Windows 10 | DASH BBB VOD | `levelSwitch` payload contract check | pass | `levelSwitch` emitted `{ tech, mediaType, from, to, bitrateKbps, width, height, codec }` instead of dash.js internal MPD/Representation objects |
| 2026-05-17 | Codex | Chromium 148.0.7778.168 / Windows 10 | Demo observability log | `network.code` / `qos.code` visibility | pass | Vite demo on `http://127.0.0.1:4174/basic.html`; demo log showed `qos` with `code: "WEBCODECS_CONFIG"`, `tech: "file"`, `codec: "avc1.640032"` from local MP4 WebCodecs configuration; HLS warning events showed `code: "HLS_WARNING"` for `bufferStalledError` and `bufferSeekOverHole`; controlled WHEP-without-backend failure showed `code: "WEBRTC_SIGNAL_ERROR"` and reconnect follow-up `code: "RECONNECT_ATTEMPT"`. This validates observability event visibility, not MediaMTX/WebRTC playback success. |
| 2026-05-17 | Codex | Jest unit test / Windows | fMP4 Tech direct queue | pending append backpressure and quota cleanup | pass | `pnpm exec jest tests/fmp4-tech.test.ts --runInBand`; verified bounded pending queue, `drop-oldest` overflow, fail-fast `error` strategy, `QuotaExceededError` cleanup/requeue, and retry exhaustion behavior. Real HTTP/WS fMP4 browser stream remains pending because no project-specific fMP4 source is available. |
| 2026-05-19 | Codex | Jest + TypeScript / Windows | fMP4 HTTP direct lifecycle | non-blocking live response pump | pass | `cmd /c pnpm exec jest tests/fmp4-tech.test.ts --runInBand` and `cmd /c pnpm exec tsc -p tsconfig.json --noEmit --pretty false` passed. Added coverage where a never-ending HTTP response body resolves `FMP4Tech.load()` after response/MSE readiness, emits `ready`, and appends the first chunk through the background pump. This fixes direct HTTP fMP4 live load semantics but does not replace real browser stream evidence. |
| 2026-05-17 | Codex | Jest unit test / Windows | GB28181 gateway adapter | invite/control contract and FLV/TS MSE dispatch | pass | `pnpm exec jest tests/gb28181.tech.test.ts --runInBand`; verified invite request/auth config, response mapping, FLV vs TS MSE dispatch, PTZ/BYE/query/keepalive control calls, missing endpoint error, and invite HTTP auth diagnostics. Real GB backend/device verification remains pending. |
| 2026-05-17 | Codex | Jest unit test / Windows | Performance monitor plugin + built-in stats | budget contract and FPS sampling | pass | `pnpm exec jest tests/performance-plugin.test.ts tests/abstract-tech-stats.test.ts --runInBand`; verified normalized samples, cumulative-counter mode, budget violations, `PERFORMANCE_BUDGET` QoS emission, cooldown, teardown, and HTML video FPS delta sampling. Real long-run browser profiling remains pending. |
| 2026-05-17 | Codex | Chrome 148.0.0.0 / Windows 10 | MediaMTX HLS local `http://127.0.0.1:8888/live/test/index.m3u8` | OBS RTMP -> MediaMTX HLS -> FyraPlayer/hls.js | pass | OBS published to `rtmp://127.0.0.1:1935/live` with stream key `test`; Vite demo on `http://127.0.0.1:4185/basic.html`; HLS playlist, init segments, and media parts returned 200; reached `ready`, `play`, `stats`, `1280x720`, about 2 Mbps and about 30 fps after user play; non-fatal hls.js startup warnings were recorded as `HLS_WARNING`. |
| 2026-05-17 | Codex | Chrome 148.0.0.0 / Windows 10 | MediaMTX WebRTC WHEP local `http://127.0.0.1:8889/live/test/whep` | OBS RTMP -> MediaMTX WHEP -> FyraPlayer WebRTC | pass | WHEP POST returned 201; ICE reached `checking -> connected`; playback reached `readyState=4`, `currentTime=10.627s`, `1280x720`; events included `ready=1`, `stats` with `bitrateKbps=2365`, `fps=30`, `rttMs=1`, `packetLoss=0`, `candidateType='host'`, `transport='udp'`; no fatal `network` events. |
| 2026-05-17 | Codex | Chrome 148.0.0.0 / Windows 10 | MediaMTX WebRTC WHEP local `http://127.0.0.1:8889/live/test/whep` | destroy -> recreate | pass | Two sequential WHEP loads after player destroy both reached `readyState=4`, `1280x720`, `readyCount=1`, `errorCount=0`, `videoErrorNetworkCount=0`, `fatalNetworkCount=0`; second run reported `fps=28`, `rttMs=1`, `packetLoss=0`, no public empty-source video error after cleanup fix. |
| 2026-05-17 | Codex | Jest unit test / Windows | WebRTC Tech stats + cleanup | MediaMTX validation regression coverage | pass | `pnpm exec jest tests/webrtc-tech-stats.test.ts --runInBand`; verified RTC stats fall back to video element dimensions, `ready` de-duplication, and cleanup of video callbacks/srcObject on destroy. |
| 2026-05-17 | Codex | Chrome 148.0.0.0 / Windows 10 | MediaMTX HLS local `http://127.0.0.1:8888/live/test/index.m3u8` | repeated HLS reload after reported duplicated audio | pass | After HLS cleanup hardening and demo command serialization, three repeated loads stayed at `videoCount=1`, `audioCount=0`, `fyra-ui-shell=1`, `state='playing'`, `muted=false`, `readyState=4`, `1280x720`; no front-end duplicate media element or UI shell accumulation was observed. The later confirmed root cause for audible layering was OBS desktop-audio capture feedback. |
| 2026-05-17 | Codex | Chrome 148.0.0.0 / Windows 10 | MediaMTX WebRTC WHEP local `http://127.0.0.1:8889/live/test/whep` | WebRTC audio no-sound investigation | partial | Player-side forced mute and extra `AudioContext` output path were removed. Browser state showed `video.muted=false`, one `video`, zero `audio`, and one WebRTC audio track, but the track was `muted=true` and `webkitAudioDecodedByteCount=0`; this points to source/server codec delivery rather than player volume. Added `WEBRTC_AUDIO_MUTED` diagnostic for this state. |
| 2026-05-18 | Codex | Jest + TypeScript / Windows | MediaMTX HLS config + Player reconnect lifecycle | HLS stability boundary and reconnect regression | pass | Added `tests/hls-config.test.ts` and Player regressions. Normal HLS now explicitly uses `lowLatencyMode: false`, `liveSyncMode: 'buffered'`, `liveSyncDurationCount: 3`, `liveMaxLatencyDurationCount: 6`, and bounded buffer while leaving hls.js audio remux/gap defaults intact. Fatal network reconnect no longer marks the active Tech as failed, resets failed Techs before retry, clears a pending reconnect timer on `ready`, skips stale reload when same-source playback is healthy and time advances, and cancels old pending reconnect when switching source. `cmd /c pnpm exec jest tests/hls-config.test.ts tests/player.test.ts --runInBand` passed; TypeScript validation passed. Real MediaMTX long-run/interruption retest remains pending. |
| 2026-05-18 | Codex | Chrome 148.0.0.0 / Windows 10 | MediaMTX HLS local `http://127.0.0.1:8888/live/test/index.m3u8` | runtime HLS config smoke after OBS feedback investigation | pass | Vite demo on `http://127.0.0.1:4185/basic.html`; active source `lowLatency=false`, current Tech `hls`, hls.js runtime config was `lowLatencyMode=false`, `liveSyncMode='buffered'`, `liveSyncDurationCount=3`, `liveMaxLatencyDurationCount=6`, `maxBufferLength=12`, `backBufferLength=30`. hls.js audio/gap defaults remained active (`maxAudioFramesDrift=1`, `nudgeOnVideoHole=true`). Playback stayed `playing`, `readyState=4`, `1280x720`, one `video`, zero extra `audio`, `webkitAudioDecodedByteCount` increased from `167758` to `188130`, and dropped frames stayed 0. hls.js still surfaced non-fatal `levelLoadTimeOut` / `audioTrackLoadTimeOut` warnings against MediaMTX sub-playlists; interruption/long-run confirmation remains pending. |
| 2026-05-18 | Codex | Chrome 148.0.0.0 / Windows 10 | MediaMTX LL-HLS local `http://127.0.0.1:8888/live/test/index.m3u8` | explicit `lowLatency: true` smoke | pass | Demo preset `MediaMTX LL-HLS local (live/test)` selected; runtime source had `lowLatency=true`, current Tech `hls`, hls.js runtime config had `lowLatencyMode=true`, `liveSyncMode='edge'`, `liveSyncDurationCount=1`, `liveMaxLatencyDurationCount=3`, `maxBufferLength=4`, `maxMaxBufferLength=8`, `backBufferLength=0`; playback reached `playing`, `readyState=4`, `1280x720`, `webkitAudioDecodedByteCount` increased from `156062` to `174281`, and dropped frames were 5 / 254 frames in the short muted smoke. Validation was muted/stopped after the run to avoid feeding browser audio back into OBS. Browser console recorded transient 500s from MediaMTX `_HLS_skip=YES` video/audio sub-playlist requests, surfaced as non-fatal hls.js warnings. |
| 2026-05-18 | Codex | Chrome 148.0.0.0 / Windows 10 | UI quality selector | native dropdown contrast | pass | Served `examples` from `http://127.0.0.1:4187/basic.html`; Shadow DOM quality selector remained white text on the dark control bar, and native popup rows had `option` computed style `color=rgb(17, 24, 39)` and `background=rgb(255, 255, 255)`, fixing the white-on-white dropdown text issue. |
| 2026-05-18 | Codex | Chrome 148.0.0.0 / Windows 10 | DASH BBB `https://dash.akamaized.net/akamai/bbb_30fps/bbb_30fps.mpd` | quality API/UI manual -> Auto | pass | Served `examples` from `http://127.0.0.1:4187/basic.html`; DASH exposed 6 quality options from `180p 254 kbps` through `432p 1884 kbps`; manual UI switch to level `5` changed `getQualityState()` to `auto=false`, `current=5`, active level 5; switching back to `Auto` changed `auto=true`. Popup option style remained dark text on white background. |
| 2026-05-18 | Codex | Chrome 148.0.0.0 / Windows 10 | Mux HLS `https://test-streams.mux.dev/x36xhzz/x36xhzz.m3u8` | quality API/UI manual -> Auto | pass | Served `examples` from `http://127.0.0.1:4187/basic.html`; HLS exposed 5 quality options from `184p 246 kbps` through `1080p 6222 kbps`; manual UI switch to 1080p changed `getQualityState()` to `auto=false`, `current=4`, active level 4; switching back to `Auto` changed `auto=true`. The Apple fMP4 HLS sample was not used for this evidence because Chrome blocked it by CORS from the local demo origin. |
| 2026-05-18 | Codex | TypeScript/Jest/esbuild / Windows | UI interruption copy + Apple HLS fMP4/CMAF sample + long-run helper | code/docs contract check | pass | Added generic UI interruption/reconnect messages, explicit Apple HLS fMP4/CMAF demo preset, and `window.fyraLongRun` sampling helper. `pnpm exec tsc -p tsconfig.json --noEmit --pretty false`, `pnpm exec jest tests/ui-components.test.ts tests/player.test.ts --runInBand`, `pnpm bundle:examples`, and `pnpm check:sources` passed. Browser playback evidence for the Apple sample remains pending because prior local Chrome demo testing was CORS-limited. |
| 2026-05-18 | Codex | TypeScript/Jest / Windows | Diagnostics plugin | snapshot/export contract | pass | Added `createDiagnosticsPlugin()` with state/source/Tech/quality/latest events/reconnect/ICE/buffer snapshot and JSON export. `pnpm exec tsc -p tsconfig.json --noEmit --pretty false`, `pnpm exec jest tests/diagnostics-plugin.test.ts --runInBand`, and `pnpm check:public-api` passed. |
| 2026-05-18 | Codex | TypeScript/Jest / Windows | Debug Panel + auth/signing middleware | product diagnostics and commercial access baseline | pass | Added `createDebugPanelPlugin()` with lightweight DOM panel/export/clear lifecycle cleanup and `createAuthSigningMiddleware()` for request/signal headers, credentials, tokens, signed URLs, and refreshed headers. Request config is consumed by HLS, HTTP fMP4, and WebRTC WHEP/WHIP; DASH custom headers are wired with credentials still deployment-dependent. `pnpm exec tsc -p tsconfig.json --noEmit --pretty false`, `pnpm exec jest tests/diagnostics-plugin.test.ts tests/auth-plugin.test.ts tests/player.test.ts --runInBand`, and `pnpm check:public-api` passed. |
| 2026-05-18 | Codex | TypeScript/Jest / Windows | Playback preferences + UI status controls | storage preference and product UI baseline | pass | Added `PlayerPreferenceEvent`, opt-in `createStoragePlugin()` persistence for source index, volume, muted state, playback speed, quality mode, and low-latency preference, plus UI preference events. Optional UI now includes an interruption/reconnect status layer, network/retry detail, and retry button after reconnect exhaustion; DOM listeners are cleanup-tracked. `pnpm exec tsc -p tsconfig.json --noEmit --pretty false`, `pnpm exec jest --runTestsByPath tests/ui-components.test.ts tests/storage-reconnect-plugin.test.ts tests/player.test.ts --runInBand`, and `pnpm check:public-api` passed. |
| 2026-05-18 | Codex | TypeScript/Jest/esbuild / Windows | UI diagnostics, screenshot, and recording hooks | commercial UI control contract | pass | Optional UI now exposes `onDiagnostics`, screenshot download/feedback with `onScreenshot`, and `showRecordingButton` / `onRecordToggle` without implementing product-owned recording. Hook event types are exported through `@bimccfyra/fyraplayer/plugins/ui-components`; disabled optional controls remain hidden across responsive layout changes. Full follow-up validation passed: `pnpm exec jest --runInBand` (23 suites / 114 tests), `pnpm build`, `pnpm bundle:examples`, `pnpm check:public-api`, `pnpm check:exports` (26 package export files), `pnpm check:sources` (17 example sources), and `git diff --check`. |
| 2026-05-18 | Codex | TypeScript/Jest / Windows | Recording backend API plugin | backend recording control contract | pass | Added `createRecordingApiPlugin()` with start/stop/status backend calls, typed `recording` events, lifecycle cleanup, and UI hook integration path without browser-side recording. `pnpm exec tsc -p tsconfig.json --noEmit --pretty false`, `pnpm exec jest tests/recording-plugin.test.ts --runInBand`, and `pnpm check:public-api` passed. |
| 2026-05-18 | Codex | TypeScript/Jest / Windows | WebRTC WHEP/WHIP hardening | timeout / HTTP / answer / ICE diagnostics | pass | Added WHEP/WHIP signaling timeout and ICE-gathering timeout support, plus normalized `network.code` values for HTTP failure, signaling timeout, answer SDP failure, and ICE gathering timeout. `pnpm exec tsc -p tsconfig.json --noEmit --pretty false`, `pnpm exec jest tests/webrtc-signaling.test.ts tests/webrtc-tech-stats.test.ts --runInBand`, and `pnpm check:public-api` passed. |
| 2026-05-18 | Codex | TypeScript/Jest / Windows | WebRTC ICE recovery contract | disconnected/failed recovery semantics | pass | Added unit-covered behavior for ICE `disconnected` grace-period recovery, optional `restartIce()`, fatal `WEBRTC_ICE_RECONNECT_REQUIRED` source reload through Player reconnect, recovery timer cancellation on `connected`/`completed`, and immediate fatal `WEBRTC_ICE_FAILED` on failed state. `cmd /c pnpm exec jest tests/webrtc-tech-stats.test.ts tests/webrtc-signaling.test.ts tests/player.test.ts --runInBand`, `cmd /c pnpm exec tsc -p tsconfig.json --noEmit --pretty false`, and `cmd /c pnpm check:public-api` passed. Real MediaMTX interruption, TURN relay, Edge, Opus audio, and long-run evidence remain pending. |
| 2026-05-18 | Codex | Chrome 148.0.0.0 / Windows 10 | MediaMTX WebRTC WHEP local `http://127.0.0.1:8889/live/test/whep` | OBS -> MediaMTX -> FyraPlayer WebRTC startup + audio RTP | pass | Served FyraPlayer examples on `http://127.0.0.1:4191/basic.html`, selected the `MediaMTX WebRTC WHEP local (live/test)` preset, and kept the browser video muted to avoid feeding playback audio back into OBS. Playback reached player `ready`, video `readyState=4`, `currentTime=10.002s`, `1280x720`, no media element error, and both audio/video MediaStream tracks were `live` with `muted=false`. RTCStats later showed inbound audio codec payload `111` (`minptime=10;useinbandfec=1`, Opus-compatible), audio packets/bytes increasing, video 1280x720, and host/UDP candidate path. This validates browser audio RTP delivery; speaker-output listening was intentionally not performed during OBS capture. |
| 2026-05-18 | Codex | Chrome 148.0.0.0 / Windows 10 | MediaMTX WebRTC WHEP local `http://127.0.0.1:8889/live/test/whep` | real browser ICE disconnected recovery | pass | During the same WHEP run, ICE moved to `disconnected`, emitted non-fatal `WEBRTC_DISCONNECTED`, then `WEBRTC_ICE_RESTART`, fatal `WEBRTC_ICE_RECONNECT_REQUIRED`, and player `RECONNECT_ATTEMPT 1/5`. The previous connection reached failed, the player reloaded the same WHEP source, ICE moved `checking -> connected`, and playback recovered to player `playing`, `readyState=4`, `currentTime=37.176s`, `1280x720`, audio/video tracks `live`, recent stats around 30fps, 1556-2181 kbps, RTT 1ms, packet loss 0. This is real observed browser recovery; controlled OBS/MediaMTX stop-start interruption evidence remains pending. |
| 2026-05-18 | Codex | Chrome 148.0.0.0 / Windows 10 | MediaMTX WebRTC WHEP local `http://127.0.0.1:8889/live/test/whep` | backend unavailable / WHEP 404 handling | pass | A later clean-tab retry returned MediaMTX `404` with `no stream is available on path 'live/test'`. FyraPlayer normalized this as `WEBRTC_WHEP_HTTP_ERROR`, emitted `WEBRTC_SIGNAL_ERROR`, retried 5 times through the player reconnect policy, and ended with `RECONNECT_EXHAUSTED`. This validates browser-visible WHEP abnormal-response handling; it does not indicate a player playback regression because the server reported no active published stream. |
| 2026-05-18 | Codex | TypeScript/Jest/docs / Windows | SDK release and integration | CR-025 consumption contract | pass | README now links the SDK release/integration guide and release scripts; `docs/sdk-release-integration.md` documents public entrypoints, plugin subpaths, all-in-one IIFE usage, release checklist, version policy, and migration boundaries. Added `pnpm check:release`. `pnpm check:release` passed, and the browser IIFE smoke on `http://127.0.0.1:4188/examples/minimal-iife.html` confirmed `window.FyraPlayerSDK` exposes `FyraPlayer`, UI, recording API, diagnostics, auth/signing, storage, and performance plugin factories with the demo reaching `playing`. |
| 2026-05-18 | Codex | TypeScript/Jest / Windows | Recording backend API plugin | CR-027 structured error contract | pass | Added `RecordingApiError`, `PlayerRecordingCode`, and `PlayerRecordingErrorInfo`; recording events now include stable codes for HTTP errors, timeouts, aborts, request failures, parse failures, and config errors. `pnpm exec tsc -p tsconfig.json --noEmit --pretty false` and `pnpm exec jest tests/recording-plugin.test.ts --runInBand` passed. This is backend API hook evidence; browser-side recording remains intentionally out of scope. |
| 2026-05-18 | Codex | TypeScript/Jest / Windows | Auth recovery plugin | CR-022 token-expiry recovery contract | pass | Added `createAuthRecoveryPlugin()` with default explicit 401/403 matching, custom matcher support, app-owned `refresh()` hook, current-source reload through `switchSource(currentIndex)`, cooldown/max retry/in-flight guards, lifecycle cleanup, and stable `AUTH_RECOVERY_*` network codes. `pnpm exec tsc -p tsconfig.json --noEmit --pretty false`, `cmd /c pnpm exec jest tests/auth-plugin.test.ts --runInBand`, `pnpm check:public-api`, and `pnpm check:exports` passed. Real backend auth recovery browser evidence remains pending. |
| 2026-05-18 | Codex | TypeScript/docs / Windows | Render bridge boundary | CR-026 external renderer contract | pass | Added `docs/render-bridges.md` documenting external PSV/Cesium/map/panorama ownership, supported `HTMLVideoElement` / player event / metadata / diagnostics / `CanvasFrameBuffer` / `BaseTarget` outputs, lifecycle pattern, and renderer verification checklist. Public API smoke now covers `CanvasFrameBuffer` and `BaseTarget`; `cmd /c pnpm check:public-api` passed. Concrete renderer browser evidence remains external/project-specific. |
| 2026-05-19 | Codex | Edge 148.0.3967.70 headless / Windows 10 | HLS VOD demo | Edge CDP smoke | pass | Served examples from `http://127.0.0.1:4192/basic.html`, selected `HLS demo`, and drove Edge through CDP. Playback used `hls` Tech, reached `readyState=4`, advanced to `currentTime=0.460s`, rendered `1280x720`, buffered about 0.04-6.016s, reported 15 total / 0 dropped frames, and emitted no fatal `network` or `error` events. |
| 2026-05-19 | Codex | Edge 148.0.3967.70 headless / Windows 10 | Apple HLS fMP4/CMAF sample | Edge HLS/fMP4 segment smoke | pass | Selected `Apple HLS fMP4/CMAF sample` (`https://devstreaming-cdn.apple.com/videos/streaming/examples/img_bipbop_adv_example_fmp4/master.m3u8`). Playback used `hls` Tech, reached `readyState=4`, advanced to `currentTime=0.570s`, rendered `768x432`, buffered about 0.033-5.951s, reported 19 total / 0 dropped frames, and exposed 24 ABR levels from 270p through 1080p. hls.js emitted one non-fatal `HLS_WARNING` with `bufferSeekOverHole`. This validates HLS playback with fMP4/CMAF segments in Edge, not the direct no-manifest `FMP4Tech`. |
| 2026-05-19 | Codex | Edge 148.0.3967.70 headless / Windows 10 | DASH BBB VOD | Edge CDP smoke + quality state | pass | Selected `DASH bbb`; playback used `dash` Tech, reached `readyState=4`, advanced to `currentTime=0.460s`, rendered `768x432`, buffered about 0.033-35.924s, reported 18 total / 0 dropped frames, and emitted no fatal `network` or `error` events. `getQualityState()` exposed 5 DASH representations, with ABR active on level 2. |
| 2026-05-19 | Codex | Edge 148.0.3967.70 headless / Windows 10 | MP4 demo | Edge CDP smoke | pass | Selected `MP4 demo`; playback used `file` Tech, reached `readyState=4`, advanced to `currentTime=0.460s`, rendered `480x270`, buffered 0-4s, reported 19 total / 0 dropped frames, and emitted no fatal `network` or `error` events. |
| 2026-05-19 | Codex | Edge 148.0.3967.70 headless / Windows 10 | MediaMTX WebRTC WHEP local `http://127.0.0.1:8889/live/test/whep` | backend unavailable / WHEP 404 handling | pass | Clean Edge tab selected the local WHEP preset while MediaMTX returned `404` with `no stream is available on path 'live/test'`. FyraPlayer normalized each POST failure as `WEBRTC_WHEP_HTTP_ERROR`, emitted `WEBRTC_SIGNAL_ERROR`, and retried through `RECONNECT_ATTEMPT` 1/5 through 5/5 before ending in `error`. This validates Edge abnormal-response handling only; it is not Edge WHEP playback-success evidence because the server reported no active published stream. |
| 2026-05-19 | Codex | Edge 148.0.3967.70 headless / Windows 10 | Local MP4 `/testvideo/Rec%200017.mp4` | `pnpm long-run:browser` runner smoke | pass | Added and smoke-tested the CDP long-run runner. Command: `pnpm long-run:browser -- --browser-path "C:\Program Files (x86)\Microsoft\Edge\Application\msedge.exe" --source-url "/testvideo/Rec%200017.mp4" --source-type file --duration 8s --interval 2s --out .fyra-long-run\smoke-local-mp4-edge.json --fail-on-error`. Summary: 6 samples, `file` Tech, state `playing`, `readyState=4`, `currentTime` advanced 0 -> 7.2666s, `1920x1200`, 217 total / 9 dropped frames, heap delta about 0.4 MB, DOM counts stable, no fatal network or error events. This validates the runner, not a 30-minute commercial long-run claim. |
| 2026-05-19 | Codex | Node.js report assertion / Windows | `.fyra-long-run\smoke-local-mp4-edge.json` | `pnpm long-run:assert` gate smoke | pass | Added the report assertion gate and checked the existing Edge MP4 runner report with `pnpm long-run:assert -- .fyra-long-run\smoke-local-mp4-edge.json --require-tech file --min-samples 2 --min-current-time-advance-sec 1 --max-memory-growth-mb 64`. The gate passed sample count, required Tech, final playable state, media-time advance, fatal/error event limits, dropped-frame ratio, memory growth, and DOM growth checks. This validates the assertion tooling only; commercial 30-minute HLS/WebRTC/fMP4 evidence remains pending. |
| 2026-05-19 | Codex | Edge 148.0.3967.70 headless / Windows 10 | MediaMTX HLS local custom port `http://127.0.0.1:28888/live/test/index.m3u8` | 30s published-stream live run + assertion | pass | MediaMTX API reported `live/test` ready from RTMP source on port `21935`, tracks `H264` + `MPEG-4 Audio`. `pnpm long-run:browser -- --browser-path "C:\Program Files (x86)\Microsoft\Edge\Application\msedge.exe" --source-url "http://127.0.0.1:28888/live/test/index.m3u8" --source-type hls --duration 30s --interval 5s --out .fyra-long-run\mediamtx-hls-ports28888-edge-30s.json --fail-on-error --expect-live` passed: 8 samples, `hls` Tech, `playing`, `readyState=4`, `1280x720`, currentTime `12.066999 -> 39.770447`, 837 total / 2 dropped frames, heap +2.54 MiB, DOM stable, no error or fatal network events. `pnpm long-run:assert -- .fyra-long-run\mediamtx-hls-ports28888-edge-30s.json --require-tech hls --expect-live --min-samples 6 --min-current-time-advance-sec 20 --max-memory-growth-mb 64` passed. Non-fatal hls.js warnings such as `fragGap` / `bufferStalledError` were observed but did not stop playback. |
| 2026-05-19 | Codex | Edge 148.0.3967.70 headless / Windows 10 | MediaMTX WHEP local custom port `http://127.0.0.1:28889/live/test/whep` | 30s published-stream live run + assertion | pass | `pnpm long-run:browser -- --browser-path "C:\Program Files (x86)\Microsoft\Edge\Application\msedge.exe" --source-url "http://127.0.0.1:28889/live/test/whep" --source-type webrtc --duration 30s --interval 5s --out .fyra-long-run\mediamtx-whep-ports28889-edge-30s.json --fail-on-error --expect-live` passed: 8 samples, `webrtc` Tech, ICE `checking -> connected`, `playing`, `readyState=4`, `1280x720`, currentTime `0 -> 30.021`, 901 total / 4 dropped frames, heap -0.15 MiB, DOM stable, no error or fatal network events. Final stats included about `2033 kbps`, `30 fps`, `rttMs=1`, `packetLoss=0`, host/UDP candidate path. `pnpm long-run:assert -- .fyra-long-run\mediamtx-whep-ports28889-edge-30s.json --require-tech webrtc --expect-live --min-samples 6 --min-current-time-advance-sec 20 --max-memory-growth-mb 64` passed. The report also emitted `WEBRTC_AUDIO_MUTED`; MediaMTX API showed the RTMP source audio track as `MPEG-4 Audio`, so this row validates Edge WHEP video/connection/retry-free playback, not Opus speaker-output success. |
| 2026-05-19 | Codex | Edge 148.0.3967.70 headless / Windows 10 | MediaMTX HLS local custom port `http://127.0.0.1:28888/live/test/index.m3u8` | 30-minute live run | partial | `pnpm long-run:browser -- --browser edge --source-url "http://127.0.0.1:28888/live/test/index.m3u8" --source-type hls --duration 30m --interval 10s --out .fyra-long-run\mediamtx-hls-28888-edge-30m.json --fail-on-error --expect-live` completed and produced 182 samples over 1800s. Playback stayed usable: `hls` Tech, final `readyState=4`, `1280x720`, currentTime `12.079772 -> 1665.27961`, 48180 total / 490 dropped frames, dropped-frame ratio about 1.02%, heap +5.21 MiB, DOM stable, not ended. Strict `long-run:assert` failed only the zero-error gate: 3 `error` events / 3 `HLS_FATAL` network events (`audioTrackLoadTimeOut`, `levelLoadError`) triggered 3 reconnect attempts, after which playback recovered and continued. Treat this as HLS long-run recovery evidence, not a clean zero-fatal commercial pass. |
| 2026-05-19 | Codex | Edge 148.0.3967.70 headless / Windows 10 | MediaMTX WHEP local custom port `http://127.0.0.1:28889/live/test/whep` | 30-minute live run + assertion | pass | `pnpm long-run:browser -- --browser edge --source-url "http://127.0.0.1:28889/live/test/whep" --source-type webrtc --duration 30m --interval 5s --out .fyra-long-run\mediamtx-whep-28889-edge-30m.json --fail-on-error --expect-live` completed with 362 samples over 1800s. `pnpm long-run:assert -- .fyra-long-run\mediamtx-whep-28889-edge-30m.json --require-tech webrtc --expect-live --min-samples 300 --min-duration-sec 1740 --min-current-time-advance-sec 120 --max-memory-growth-mb 256` passed. Final state was playable with `readyState=4`, `1280x720`, currentTime `0 -> 1662.559`, 46512 total / 2663 dropped frames, dropped-frame ratio about 5.73%, heap +0.38 MiB, DOM stable, no public error events, and no unresolved fatal network state. The run included one ICE disconnected/reconnect-required/reconnect-attempt sequence and recovered. `WEBRTC_AUDIO_MUTED` remained present, consistent with the RTMP/AAC ingest boundary. |
| 2026-05-19 | Codex | Edge 148.0.3967.70 headless / Windows 10 | ffmpeg direct fMP4 fixture `http://127.0.0.1:18080/stream.fmp4` | 10-minute live run | pass | Added `checks/ffmpeg-fmp4-fixture.mjs` and `pnpm serve:fmp4-fixture` to serve `G:\MTX\fmp4test.mp4` as looping fragmented MP4 with CORS. `pnpm long-run:browser -- --url http://127.0.0.1:3000/basic.html --source-url http://127.0.0.1:18080/stream.fmp4 --source-type fmp4 --duration 10m --interval 10s --out .fyra-long-run\ffmpeg-fmp4-edge-10m.json --fail-on-error --expect-live` passed: 62 samples, `fmp4` Tech, `playing`, `readyState=4`, `960x540`, currentTime `0.001774 -> 599.913408`, 17,981 total / 3 dropped frames, heap +1.53 MiB, DOM stable, no error or fatal network events. This validates direct no-manifest `FMP4Tech` against the local ffmpeg fixture; project-specific HTTP/WS fMP4 sources still need their own rows. |
| 2026-05-19 | Codex | Chrome 148.0.0.0 / Windows 10 | ffmpeg direct fMP4 fixture `http://127.0.0.1:18080/stream.fmp4` | manual playback smoke | pass | Manual browser run through `examples/basic.html` loaded the direct fMP4 source with `source-type=fmp4`. The page reported `readyState=4`, `state=playing`, `tech=fmp4`, `960x540`, and currentTime continued advancing past 327s after a 10s follow-up wait. Only a favicon 404 was observed; no fMP4 fatal network error was present. This is Chrome smoke evidence, not a Chrome long-run. |
| 2026-05-19 | Codex | Edge 148.0.0.0 headless / Windows 10 | PanoramaLite generated 2:1 image fixture | WebGL2 image render + interaction + destroy smoke | pass | Added `examples/panoramalite.html` and `pnpm smoke:panoramalite`. Command: `pnpm smoke:panoramalite -- --scenario image --duration 2s --out .fyra-long-run\panoramalite-image-edge.json --fail-on-error`. Result: canvas `695x390`, nonblank canvas `nonBlack=9`, `unique=7`, pointer drag changed view and pixels (`pixelDelta=1571`), and destroy removed the PanoramaLite canvas. |
| 2026-05-19 | Codex | Edge 148.0.0.0 headless / Windows 10 | PanoramaLite local MP4 `/testvideo/Rec%200017.mp4` | WebGL2 video texture + interaction + destroy smoke | pass | Command: `pnpm smoke:panoramalite -- --scenario file --source-url /testvideo/Rec%200017.mp4 --duration 6s --out .fyra-long-run\panoramalite-file-local-edge.json --fail-on-error`. Result: file Tech reached `playing`, canvas nonblank (`nonBlack=9`, `unique=9`), pointer drag changed view and pixels (`pixelDelta=1074` in the final run), video was ready, and destroy removed the canvas. This validates same-origin video texture upload; the external Bitmovin 360 MP4 default source failed separately with no supported source. |
| 2026-05-19 | Codex | Edge 148.0.0.0 headless / Windows 10 | PanoramaLite HLS demo `https://sf1-cdn-tos.huoshanstatic.com/obj/media-fe/xgplayer_doc_video/hls/xgplayer-demo.m3u8` | HLS/MSE video texture + interaction + destroy smoke | pass | Command: `pnpm smoke:panoramalite -- --scenario hls --source-url https://sf1-cdn-tos.huoshanstatic.com/obj/media-fe/xgplayer_doc_video/hls/xgplayer-demo.m3u8 --duration 8s --out .fyra-long-run\panoramalite-hls-demo-edge.json --fail-on-error`. Result: HLS Tech reached `playing`, canvas nonblank (`nonBlack=9`, `unique=4`), pointer drag changed view and pixels (`pixelDelta=1677`), video was ready, and destroy removed the canvas. This pass required adding media-element readiness/frame event scheduling to PanoramaLite after an initial black-frame timing failure. |
| 2026-05-19 | Codex | Edge 148.0.0.0 headless / Windows 10 | PanoramaLite MediaMTX HLS live `http://127.0.0.1:28888/live/test/index.m3u8` | live HLS video texture + interaction + destroy smoke | pass | MediaMTX API reported `live/test` ready from OBS publish and the HLS playlist returned 200. Command: `pnpm smoke:panoramalite -- --scenario hls --source-url http://127.0.0.1:28888/live/test/index.m3u8 --duration 20s --out .fyra-long-run\panoramalite-hls-live-edge.json --fail-on-error`. Result: HLS Tech reached `playing`, canvas nonblank (`nonBlack=9`, `unique=9`), pointer drag changed view and pixels (`pixelDelta=806`), video was ready, and destroy removed the canvas. An earlier 8s run timed out before nonblank canvas; the 20s run is the accepted live HLS smoke. |
| 2026-05-19 | Codex | Edge 148.0.0.0 headless / Windows 10 | PanoramaLite MediaMTX WHEP live `http://127.0.0.1:28889/live/test/whep` | live WebRTC video texture + interaction + destroy smoke | pass | Command: `pnpm smoke:panoramalite -- --scenario webrtc --source-url http://127.0.0.1:28889/live/test/whep --duration 8s --out .fyra-long-run\panoramalite-webrtc-live-edge.json --fail-on-error`. Result: WebRTC Tech reached `playing`, canvas nonblank (`nonBlack=9`, `unique=6`), pointer drag changed view and pixels (`pixelDelta=791`), video was ready, and destroy removed the canvas. |
| 2026-05-19 | Codex | Edge headless / Windows 10 | PanoramaLite generated latitude/longitude grid | orientation calibration image smoke | pass | Command: `pnpm smoke:panoramalite -- --scenario image --duration 2s --out .fyra-long-run\panoramalite-grid-image-edge.json --fail-on-error`. Result: generated 2:1 grid rendered nonblank (`nonBlack=9`, `unique=6`), pointer drag changed view and pixels (`pixelDelta=1864`), and destroy removed the canvas. This validates the new default orientation-calibration fixture. |
| 2026-05-19 | Codex | Edge headless / Windows 10 | PanoramaLite MediaMTX HLS live `http://127.0.0.1:28888/live/test/index.m3u8` | optimized live HLS panorama smoke | pass | MediaMTX API reported `live/test` ready from OBS RTMP publish with H.264 + MPEG-4 Audio. Command: `pnpm smoke:panoramalite -- --scenario hls --source-url http://127.0.0.1:28888/live/test/index.m3u8 --duration 12s --out .fyra-long-run\panoramalite-hls-live-optimized-edge.json --fail-on-error`. Result: HLS Tech reached `playing`, canvas nonblank (`nonBlack=9`, `unique=9`), pointer drag changed view and pixels (`pixelDelta=795`), video was ready, and destroy removed the canvas while demo options capped `maxPixelRatio`, `maxCanvasPixels`, and video upload fps. |
| 2026-05-19 | Codex | Edge headless / Windows 10 | PanoramaLite MediaMTX WHEP live `http://127.0.0.1:28889/live/test/whep` | optimized live WebRTC panorama smoke | pass | Command: `pnpm smoke:panoramalite -- --scenario webrtc --source-url http://127.0.0.1:28889/live/test/whep --duration 6s --out .fyra-long-run\panoramalite-webrtc-live-optimized-edge-retry.json --fail-on-error`. Result: WebRTC Tech reached `playing`, canvas nonblank (`nonBlack=9`, `unique=8`), pointer drag changed view and pixels (`pixelDelta=1161`), video was ready, and destroy removed the canvas. One prior 10s automation run failed with CDP `Promise was collected` and produced no report; the retry passed, so the accepted evidence is the retry row. |
| 2026-05-19 | Codex | Edge headless / Windows 10 | PanoramaLite generated latitude/longitude grid | default-quality orientation smoke | pass | Command: `pnpm smoke:panoramalite -- --scenario image --duration 2s --out .fyra-long-run\panoramalite-grid-image-orientation-default-quality-edge.json --fail-on-error`. Result: generated grid rendered nonblank (`nonBlack=9`, `unique=6`), pointer drag changed view and pixels (`pixelDelta=1825`), and destroy removed the canvas. This run used then-current demo defaults: `preserveDrawingBuffer` only in smoke mode, image `textureFlipY` default enabled, no default canvas-pixel cap, and no default video upload fps cap. The image-Y-flip default was superseded on 2026-05-20 because the generated grid is the zero-flip baseline. |
| 2026-05-19 | Codex | Edge headless / Windows 10 | PanoramaLite MediaMTX HLS live `http://127.0.0.1:28888/live/test/index.m3u8` | default-quality live HLS panorama smoke | pass | Command: `pnpm smoke:panoramalite -- --port 4201 --scenario hls --source-url http://127.0.0.1:28888/live/test/index.m3u8 --duration 12s --out .fyra-long-run\panoramalite-hls-live-default-quality-edge.json --fail-on-error`. Result: HLS Tech reached `playing`, canvas nonblank (`nonBlack=9`, `unique=9`), pointer drag changed view and pixels (`pixelDelta=1763`), video was ready, and destroy removed the canvas. This validates current default-quality scheduling with no demo `maxCanvasPixels` or `maxVideoFps` cap. |
| 2026-05-19 | Codex | Edge headless / Windows 10 | PanoramaLite MediaMTX WHEP live `http://127.0.0.1:28889/live/test/whep` | default-quality live WebRTC panorama smoke | pass | Command: `pnpm smoke:panoramalite -- --port 4202 --scenario webrtc --source-url http://127.0.0.1:28889/live/test/whep --duration 10s --out .fyra-long-run\panoramalite-webrtc-live-default-quality-edge.json --fail-on-error`. Result: WebRTC Tech reached `playing`, canvas nonblank (`nonBlack=9`, `unique=9`), pointer drag changed view and pixels (`pixelDelta=1818`), video was ready, and destroy removed the canvas. This validates current default-quality scheduling with no demo `maxCanvasPixels` or `maxVideoFps` cap; subjective WebRTC smoothness still depends on live network/decode jitter and should be evaluated with RTC stats during manual testing. |
| 2026-05-20 | Codex | Edge headless / Windows 10 | PanoramaLite generated latitude/longitude grid | viewer-controls image smoke | pass | Command: `pnpm smoke:panoramalite -- --port 4210 --scenario image --duration 2s --out .fyra-long-run\panoramalite-viewer-controls-image-edge.json --fail-on-error`. Result: generated grid rendered nonblank (`nonBlack=9`, `unique=9`), pointer drag changed view and pixels (`pixelDelta=955`), and destroy removed the canvas. Demo had `viewerControls: true`; image mode hides video-only controls while preserving reset/fullscreen affordance. |
| 2026-05-20 | Codex | Edge headless / Windows 10 | PanoramaLite MediaMTX HLS live `http://127.0.0.1:28888/live/test/index.m3u8` | then-current video X-default + viewer-controls live HLS smoke | pass | Command: `pnpm smoke:panoramalite -- --port 4211 --scenario hls --source-url http://127.0.0.1:28888/live/test/index.m3u8 --duration 12s --out .fyra-long-run\panoramalite-hls-video-x-default-edge.json --fail-on-error`. Result: HLS Tech reached `playing`, canvas nonblank (`nonBlack=9`, `unique=9`), pointer drag changed view and pixels (`pixelDelta=679`), video was ready, and destroy removed the canvas. This validated the then-current video-source default `textureFlipX: true` path; that default was superseded later on 2026-05-20 when the base sphere U mapping was corrected to zero-flip orientation. |
| 2026-05-20 | Codex | Edge headless / Windows 10 | PanoramaLite MediaMTX WHEP live `http://127.0.0.1:28889/live/test/whep` | then-current video X-default + viewer-controls live WebRTC smoke | pass | First automation attempt timed out at CDP `Runtime.evaluate` and produced no player report. Retry command: `pnpm smoke:panoramalite -- --port 4213 --scenario webrtc --source-url http://127.0.0.1:28889/live/test/whep --duration 8s --out .fyra-long-run\panoramalite-webrtc-video-x-default-edge-retry.json --fail-on-error`. Result: WebRTC Tech reached `playing`, canvas nonblank (`nonBlack=9`, `unique=9`), pointer drag changed view and pixels (`pixelDelta=1580`), video was ready, and destroy removed the canvas. This validated the then-current video-source default `textureFlipX: true` path; that default was superseded later on 2026-05-20 when the base sphere U mapping was corrected to zero-flip orientation. |
| 2026-05-20 | Codex | Edge headless / Windows 10 | PanoramaLite generated latitude/longitude grid | viewer-controls image smoke, PlayerAPI control path build | pass | Concurrent first run timed out waiting for nonblank canvas and produced no accepted report. Retry command: `pnpm smoke:panoramalite -- --port 4224 --scenario image --duration 3s --out .fyra-long-run\panoramalite-viewer-controls-image-edge-20260520-retry.json --fail-on-error`. Result: generated grid rendered nonblank (`nonBlack=9`, `unique=6`), pointer drag changed view and pixels (`pixelDelta=1825`), and destroy removed the canvas. |
| 2026-05-20 | Codex | Edge headless / Windows 10 | PanoramaLite MediaMTX HLS live `http://127.0.0.1:28888/live/test/index.m3u8` | then-current video X-default + viewer-controls live HLS smoke, PlayerAPI control path build | pass | MediaMTX API reported `live/test` ready from OBS RTMP publish with H.264 + MPEG-4 Audio; WHEP OPTIONS returned 204 and HLS playlist returned 200 before smoke. Command: `pnpm smoke:panoramalite -- --port 4221 --scenario hls --source-url http://127.0.0.1:28888/live/test/index.m3u8 --duration 12s --out .fyra-long-run\panoramalite-hls-video-x-default-edge-20260520.json --fail-on-error`. Result: HLS Tech reached `playing`, canvas nonblank (`nonBlack=9`, `unique=9`), pointer drag changed view and pixels (`pixelDelta=1093`), video was ready, and destroy removed the canvas. Superseded by the later zero-flip U-mapping correction. |
| 2026-05-20 | Codex | Edge headless / Windows 10 | PanoramaLite MediaMTX WHEP live `http://127.0.0.1:28889/live/test/whep` | then-current video X-default + viewer-controls live WebRTC smoke, PlayerAPI control path build | pass | First run timed out at CDP `Runtime.evaluate` and produced no accepted player report. Retry command: `pnpm smoke:panoramalite -- --port 4225 --scenario webrtc --source-url http://127.0.0.1:28889/live/test/whep --duration 10s --out .fyra-long-run\panoramalite-webrtc-video-x-default-edge-20260520-retry.json --fail-on-error`. Result: WebRTC Tech reached `playing`, canvas nonblank (`nonBlack=9`, `unique=9`), pointer drag changed view and pixels (`pixelDelta=1749`), video was ready, and destroy removed the canvas. Superseded by the later zero-flip U-mapping correction. |
| 2026-05-20 | Codex | Edge headless / Windows 10 | PanoramaLite generated latitude/longitude grid | zero-flip orientation baseline after U-mapping correction | pass | Browser screenshot review on the Vite demo showed `Flip X` and `Flip Y` both unchecked with readable `FRONT 0deg` and latitude labels. Command: `pnpm smoke:panoramalite -- --port 4230 --scenario image --duration 3s --out .fyra-long-run\panoramalite-image-zero-flip-baseline-edge-20260520.json --fail-on-error`. Result: generated grid rendered nonblank (`nonBlack=9`, `unique=6`), pointer drag changed view and pixels (`pixelDelta=1564`), and destroy removed the canvas. This is the accepted zero-flip baseline after changing sphere U coordinates from `1 - u` to `u` and setting image/video flip defaults to `false/false`. |
| 2026-05-20 | Codex | Edge headless / Windows 10 | PanoramaLite Naver equirectangular HLS `https://naver.github.io/egjs-view360/pano/equirect/m3u8/equi.m3u8` | new public panorama HLS preset smoke | pass | Playlist returned 200 with `Access-Control-Allow-Origin: *`. Command: `pnpm smoke:panoramalite -- --port 4231 --scenario hls --source-url https://naver.github.io/egjs-view360/pano/equirect/m3u8/equi.m3u8 --duration 8s --out .fyra-long-run\panoramalite-hls-naver-equirect-edge-20260520.json --fail-on-error`. Result: HLS Tech reached `playing`, canvas nonblank (`nonBlack=9`, `unique=8`), pointer drag changed view and pixels (`pixelDelta=744`), video was ready, and destroy removed the canvas. |
| 2026-05-20 | Codex | Edge headless / Windows 10 | PanoramaLite Radiant Lac de Bimont HLS `https://cdn.radiantmediatechs.com/rmp/media/samples-for-rmp-site/04052024-lac-de-bimont/hls/playlist.m3u8` | new public panorama HLS preset smoke | pass | Playlist returned 200 with `Access-Control-Allow-Origin: *`. Command: `pnpm smoke:panoramalite -- --port 4232 --scenario hls --source-url https://cdn.radiantmediatechs.com/rmp/media/samples-for-rmp-site/04052024-lac-de-bimont/hls/playlist.m3u8 --duration 8s --out .fyra-long-run\panoramalite-hls-radiant-lac-de-bimont-edge-20260520.json --fail-on-error`. Result: HLS Tech reached `playing`, canvas nonblank (`nonBlack=9`, `unique=9`), pointer drag changed view and pixels (`pixelDelta=601`), video was ready, and destroy removed the canvas. |
| 2026-05-20 | Codex | Edge headless / Windows 10 | PanoramaLite Naver equirectangular HLS `https://naver.github.io/egjs-view360/pano/equirect/m3u8/equi.m3u8` | runtime mode API + public panorama HLS preset smoke | pass | Command: `pnpm smoke:panoramalite -- --port 4233 --scenario hls --source-url https://naver.github.io/egjs-view360/pano/equirect/m3u8/equi.m3u8 --duration 8s --out .fyra-long-run\panoramalite-hls-naver-equirect-edge-20260520-runtime-mode.json --fail-on-error`. Result: HLS Tech reached `playing`, canvas nonblank (`nonBlack=9`, `unique=8`), pointer drag changed view and pixels (`pixelDelta=744`), video was ready, and destroy removed the canvas. Unit coverage also verifies `enabled: false` -> `handle.setEnabled(true/false)` toggles current video rendering without source reload. |
| 2026-05-20 | Codex | Edge headless / Windows 10 | PanoramaLite Radiant Lac de Bimont HLS `https://cdn.radiantmediatechs.com/rmp/media/samples-for-rmp-site/04052024-lac-de-bimont/hls/playlist.m3u8` | runtime mode API + public panorama HLS preset smoke | pass | Command: `pnpm smoke:panoramalite -- --port 4234 --scenario hls --source-url https://cdn.radiantmediatechs.com/rmp/media/samples-for-rmp-site/04052024-lac-de-bimont/hls/playlist.m3u8 --duration 8s --out .fyra-long-run\panoramalite-hls-radiant-lac-de-bimont-edge-20260520-runtime-mode.json --fail-on-error`. Result: HLS Tech reached `playing`, canvas nonblank (`nonBlack=9`, `unique=9`), pointer drag changed view and pixels (`pixelDelta=560`), video was ready, and destroy removed the canvas. |
| 2026-05-20 | Codex | Edge headless / Windows 10 | PanoramaLite Electroteque Ultra Light Flight HLS `https://videos.electroteque.org/360/hls/ultra_light_flight.m3u8` | new public panorama HLS preset smoke | pass | Playlist returned 200. Command: `pnpm smoke:panoramalite -- --port 4235 --scenario hls --source-url https://videos.electroteque.org/360/hls/ultra_light_flight.m3u8 --duration 8s --out .fyra-long-run\panoramalite-hls-electroteque-ultra-light-flight-edge-20260520.json --fail-on-error`. Result: HLS Tech reached `playing`, canvas nonblank (`nonBlack=9`, `unique=9`), pointer drag changed view and pixels (`pixelDelta=1183`), video was ready, and destroy removed the canvas. |
| 2026-05-20 | Codex | Edge headless + Playwright / Windows 10 | PanoramaLite generated latitude/longitude grid | screen interaction keeps roll stable | pass | Removed the temporary Z-axis locking UI/API after product review. Command: `pnpm smoke:panoramalite -- --port 4242 --scenario image --duration 3s --out .fyra-long-run\panoramalite-screen-roll-stable-image-edge-20260520.json --fail-on-error` passed with nonblank canvas, drag view/pixel changes, clean destroy, and `rollStableAfterDrag=true`. Playwright on `http://127.0.0.1:4240/panoramalite.html` confirmed the locking switch is absent, status shows `roll 0.0`, and pointer drag changed `yaw/pitch` while `rollDelta=0`. |
| 2026-05-20 | Codex | Playwright Chromium / Windows 10 | Unified main demo `http://127.0.0.1:4246/basic.html` | ordinary/PanoramaLite source list and runtime UI replacement | pass | Vite served the current bundled demo on port 4246. Playwright confirmed the unified source selector has three `[全景]` sources; selecting `[全景] Panorama HLS - Naver equirect` filled the HLS URL, checked the panorama toggle, set `.player-shell.panorama-mode`, created one PanoramaLite canvas, reported `handle.isEnabled() === true`, and hid `fyra-ui-shell`. Disabling panorama mode set `handle.isEnabled() === false`, hid the canvas, restored the UI shell, and hid panorama-only options. |
| 2026-05-20 | Codex | Playwright Chromium / Windows 10 | Unified main demo `http://127.0.0.1:4247/basic.html` | source presentation metadata triggers PanoramaLite | pass | After replacing demo-only `panorama: true` presets with formal `presentation` metadata, Playwright selected `[全景] Panorama HLS - Naver equirect`. `player.getCurrentSource()` exposed `presentation: { mode: 'panorama', projection: 'equirectangular', renderer: 'panoramalite' }` and `tags: ['panorama']`; the panorama toggle was checked, `handle.isEnabled() === true`, `.player-shell.panorama-mode` was set, `fyra-ui-shell` was hidden, the PanoramaLite canvas was displayed, and the status showed `PanoramaLite: active / flipX=off / flipY=off`. |

---

## 7. Promotion Rules

- A protocol is `verified` only after at least Chrome and Edge pass, unless the browser scope explicitly excludes it.
- Safari support must be recorded separately because native HLS and WebCodecs/MSE behavior differ.
- Public docs should call a source/protocol `experimental` until at least one real browser run exists in this file.
- GB28181 means server-side gateway integration. The browser player does not implement SIP/RTP/PS; placeholders are acceptable, but they do not count as commercial verification.
